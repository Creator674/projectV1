import { SEMANTIC_COLORS } from "@/theme/colors";

export const PROGRESSBAR_CONFIG = {
  color: SEMANTIC_COLORS.purple,
  startPosition: 0.2,
  height: 3,
  showOnShallow: true,
  options: {
    showSpinner: false
  }
};